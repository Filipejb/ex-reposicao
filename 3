#include <stdio.h>

int main() {
    int n, i;

    printf("Digite um numero inteiro: ");
    scanf("%d", &n);

    printf("Os primeiros %d numeros naturais impares sao:\n", n);

    for(i = 1; i <= 2*n-1; i += 2) {
        printf("%d ", i);
    }

    printf("\n");

    return 0;
}
